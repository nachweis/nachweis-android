<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark-mode launch window: paint ink so the pre-Compose frame is not a bright flash. -->
    <style name="Theme.Nachweis" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/ink</item>
        <item name="android:statusBarColor">@color/ink</item>
    </style>
</resources>
