# nachweis production trust anchors (placeholder)
#
# Real public production trust anchors are provisioned into this flavor at
# build/deployment time. Only public anchors ever live here; no CA private keys.
#
# This placeholder exists so the production flavor packages a trust-root resource
# distinct from the demo flavor. It is intentionally empty of certificates.
