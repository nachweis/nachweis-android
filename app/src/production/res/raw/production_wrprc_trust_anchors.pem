# Public root for the production nachweis WRPRC-provider trust bundle.
# NOT deployed. The WRPRC-provider signing key is a distinct actor from the WRPAC CA and its
# private key remains offline. Empty of certificates until published, so WRPRC verification
# fails closed in production too.
